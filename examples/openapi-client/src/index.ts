/**
 * `@ascended/example-openapi-client`
 *
 * Example client consuming the reference SDK (`@ascended/sdk`) against the
 * reference minimal server (`@ascended/example-minimal-server`).
 */

export { runApiDemo, type ApiDemoResult, type DemoStep } from "./demo.js";
